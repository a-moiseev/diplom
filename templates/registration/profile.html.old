{% extends "base.html" %}

{% block title %}Профайл: {{ prof }}{% endblock %}

{% block content %}
    <h1>{{ prof }}</h1>
    
    {% if user.teacher %}
    {# Если преподаватель#}
        {% include "registration/profile_teacher.html" %}
    {% endif %}

    {% if user.student %}
    {# Если student #}
        {% include "registration/profile_student.html" %}
    {% endif %}

{% endblock %}